import { CommonRepository } from '../shared/common/repository.common.js';

class Repository extends CommonRepository {
    constructor(model){
        super(model)
    }
}

export default Repository;
